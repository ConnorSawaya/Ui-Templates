# Uploaded References

Place shared uploaded reference images here.

Suggested naming:

- `duolingo-reference-board.png`
- `current-ui-screenshot.png`

If a reference belongs to a specific template, also copy it into that template's `references/` folder.
